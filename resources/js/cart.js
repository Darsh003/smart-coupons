// resources/js/cart.js
// Global cart utilities — auto-update cart count badge on page load

document.addEventListener('DOMContentLoaded', () => {
    // Any page-level cart utilities go here.
    // The per-page AJAX logic is embedded directly in Blade @push('scripts').
});
